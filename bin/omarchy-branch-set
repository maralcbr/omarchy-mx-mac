#!/bin/bash

# omarchy:summary=Set the branch for Omarchy's git repository.
# omarchy:args=<master|rc|dev>

if (($# == 0)); then
  echo "Usage: omarchy-branch-set [main|dev]"
  exit 1
else
  branch="$1"
fi

case "$branch" in
  "main") git -C $OMARCHY_PATH switch main ;;
  "dev") git -C $OMARCHY_PATH switch dev ;;
  *) echo "Unknown branch: $branch"; exit 1; ;;
esac
