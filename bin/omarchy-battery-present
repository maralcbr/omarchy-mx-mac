#!/bin/bash

# omarchy:summary=Returns true if a battery is present on the system.

power_supply_path="${OMARCHY_POWER_SUPPLY_PATH:-/sys/class/power_supply}"

for bat in "$power_supply_path"/*; do
  [[ -r $bat/present && $(<"$bat/present") == "1" ]] || continue
  [[ -r $bat/type && $(<"$bat/type") == "Battery" ]] || continue
  # Peripheral batteries do not power the machine. Keep unscoped older drivers.
  [[ -r $bat/scope && $(<"$bat/scope") == "Device" ]] && continue
  exit 0
done

exit 1
