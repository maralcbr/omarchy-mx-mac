#!/bin/bash
# omarchy:summary=Set up or remove fingerprint authentication
if [[ ${1:-} == "--remove" ]]; then
  exec omarchy-remove-security-fingerprint "${@:2}"
fi

exec omarchy-setup-security-fingerprint "$@"
